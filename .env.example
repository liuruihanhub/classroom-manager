# Supabase public browser configuration. Copy to .env.local and fill locally.
# Never put service_role keys, passwords, access tokens, or private keys here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
