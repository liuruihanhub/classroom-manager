# 教师课堂管理系统 v3.0 学生教学档案需求

> 日期：2026-08-11  
> 状态：已冻结，必须在 v2.0 正式版通过后实施  
> 定位：从“按名单记录课堂数据”升级为“以学生为中心的教学观察、目标和跟进工具”。

## 1. 用户真正需要解决的问题

教师面对多个班级和多个学期时，单独的考勤表、成绩表和课堂事件不足以回答：

- 这个学生近期最需要关注什么？
- 上次与他沟通或指导了什么？
- 哪些问题需要下节课继续跟进？
- 考勤、课堂表现、作业和教师观察之间是否存在可解释的变化？
- 学期更换后，哪些长期有效的教学信息应保留，哪些历史课堂数据应保持只读？

v3.0 建立“学生教学档案”，但不建设包含大量个人隐私的学生信息库，也不做自动人格标签、风险分或 AI 结论。

## 2. 数据最小化与隐私边界

- 默认学生基础字段继续只有：姓名、可选学号、系统内部 ID。
- 不新增身份证号、家庭住址、私人电话、家长信息、医疗诊断、家庭经济情况、宗教、照片或定位字段。
- 备注输入区固定提示：只记录完成教学任务所必需的信息，不写医疗、家庭、处分等敏感细节。
- 学生档案、备注、目标和跟进均属于教师私密页面，显示“请勿投屏”。
- 导出含学生档案时单独确认并提示妥善保存。
- 所有测试、截图和文档只能使用虚构学生与虚构教学观察。

## 3. 稳定学生档案 ID 与跨学期边界

- 新增稳定 `personId`，用于跨学期识别同一名学生；每学期名单记录仍保留自己的快照 ID，避免历史报表被新学期修改污染。
- 从旧学期复制名单到新学期时保留 `personId`，但生成新的学期学生 ID。
- 新导入名单不得仅凭同名或同学号自动关联历史档案；系统可以给出候选摘要，必须由教师确认。
- 解除错误关联必须二次确认，不删除两个学期的历史课堂记录。
- 档案级信息与学期/课程级信息分开：长期教学提示可归属 person，课堂观察可归属学期和课程。

## 4. 学生档案页

从名单、全局搜索、考勤异常、预警、成绩表、抽名结果和课堂工作台都可以进入同一学生档案。

档案页至少包含：

- 基本摘要：姓名、可选学号、当前学期班级、课程；
- 教学概览：各考勤状态次数、当前成绩项和最终分、最近课堂表现；
- 最近备注、置顶教学提示和待跟进事项；
- 学习目标及完成状态；
- 按时间排序的教学时间线；
- 学期、课程、类型过滤；
- 明确的“新增教学备注”“新增学习目标”“安排跟进”入口。

档案不得生成“差生”“问题学生”“高风险”等自动标签，不用颜色或排名给学生做人格判断。

## 5. 教学备注

每条备注包含：

- 唯一 ID、`personId`；
- 可选学期、班级、课程上下文；
- 类型：课堂观察、学习困难、学习优势、作业跟进、实训表现、沟通记录、其他；
- 正文，1–1000 字；
- 可选自定义教学标签；
- 创建时间、最后修改时间；
- 是否置顶；
- 可选下次跟进日期；
- 状态：有效、已完成跟进、已归档。

规则：

- 输入按纯文本安全呈现，不执行 HTML，不支持脚本或富文本粘贴。
- 修改保留最近修改时间；归档代替直接永久删除。
- 归档后可以恢复；永久删除必须从归档区二次确认，并先生成安全副本。
- 一次课堂抽名或课堂工作台中可以快速新增预设类型备注，最多 2 次主要点击进入编辑。
- 备注排序默认“置顶优先、其余按更新时间倒序”。

## 6. 学习目标与跟进

### 学习目标

- 标题、说明、适用课程、开始日期、可选截止日期；
- 状态：未开始、进行中、已完成、已暂停；
- 可选进展记录；
- 完成时保留完成日期，不删除历史。

### 跟进事项

- 来源可以是备注、考勤预警、成绩观察或教师主动创建；
- 包含待办内容、计划日期、状态和完成说明；
- 总览和课堂工作台显示“今天/逾期待跟进”，但不做系统通知或外部消息推送；
- 完成跟进后可以关联一条新的教学备注。

## 7. 教学时间线

时间线按学生聚合以下只读事件：

- 教学备注创建/修改/完成跟进；
- 学习目标创建、进展和完成；
- 考勤异常；
- 课堂表现加减分；
- 作业/自定义成绩项变化；
- 抽名记录（可按开关显示）。

时间线只做事实聚合，不自动推断因果。教师可以按学期、课程和事件类型过滤。

## 8. 标签、关注清单与查找

- 教师可以创建中性、教学用途的自定义标签，例如“待补交”“需复习高压安全”“实训步骤需提醒”。
- 禁止内置侮辱性、歧视性或不可解释的标签。
- 支持按标签、待跟进、目标状态、班级和课程筛选学生。
- 总览新增“今日跟进”和“关注清单”，只显示教师主动置顶或存在明确待办的学生。
- 查找结果区分同名学生，至少显示学期/班级和非空学号。

## 9. 批量与课堂操作

- 名单页支持批量添加一个教学标签，但批量备注正文必须再次确认，防止误写全班。
- 考勤保存后可以对异常学生逐个快速新增跟进，不自动生成带评价的备注。
- 抽名结果支持“添加教学备注”和“添加到关注清单”，不默认改变成绩。
- 课堂工作台只显示必要的置顶提示和今天待跟进，不在投屏状态展示完整私密备注。

## 10. 同步、冲突、备份和恢复

- `personId`、备注、目标、跟进和标签进入现有 3.0 JSON 文档，通过本地优先、CAS revision 和 Realtime 同步。
- 离线新增/修改保持最终快照补传，不产生重复备注或目标。
- 同一备注被两台设备同时修改时，整库 revision 冲突必须展示两个版本摘要，不静默合并正文。
- JSON 备份包含完整档案；独立随机抽名页导入时必须忽略备注、目标、跟进和标签。
- 云历史和本机安全副本恢复继续适用，恢复前必须校验 3.0 数据引用闭合。

## 11. 数据版本与迁移

- 数据版本升级为 `3.0`。
- `2.0 -> 3.0` 为每个历史学生生成或继承稳定 `personId`；同一旧学期内不得重复，跨学期不得靠姓名自动合并。
- 旧数据迁移后，备注、目标、跟进和标签为空数组；既有名单、考勤、成绩和抽名记录全部不变。
- 连续迁移两次结果一致；迁移失败不得改动本机或云端原版本。
- SQL 结构门禁支持 3.0 的新增顶层数组并保持 payload 大小限制；若 4 MiB 不足，必须先用虚构长期数据测量后再调整，不能无上限放大。

## 12. 性能和容量

使用虚构长期数据验证：

- 360 名当前学生；
- 跨学期至少 720 个名单快照；
- 5,000 条备注、1,000 个学习目标、1,000 个跟进事项；
- 学生档案首次打开、过滤和搜索分别报告 p50/p95；目标 p95 小于 150ms（不含首次按需资源下载）；
- JSON 总大小、localStorage 占用和云 payload 大小必须报告，接近 4 MiB 时给出明确容量提示。

不得为每个键盘输入立即上传云端；继续使用本地写入和短防抖最终快照。

## 13. 可访问性与视觉

- 学生档案采用清晰摘要、时间线和侧栏/折叠区，不用无止境卡片堆叠。
- 私密信息使用文字和图标提示，不用刺眼大面积警告色。
- 备注、目标、跟进表单有明确标签、错误定位、键盘操作和取消路径。
- 375px 手机可完成查看档案、新增备注和完成跟进，无页面级横向溢出。
- 时间线和状态不能只依靠颜色表达；尊重减少动效设置。
- 全站文案继续使用教师视角，避免医疗化、惩罚化、人格化表达。

## 14. v3.0 迭代顺序

1. `3.0-alpha`：`personId`、3.0 迁移、学生档案骨架；
2. `3.0-beta1`：教学备注、归档恢复、快速入口；
3. `3.0-beta2`：学习目标、跟进、关注清单；
4. `3.0-rc1`：时间线、筛选、容量、同步冲突和移动端；
5. `3.0`：视觉文案、攻击测试、浏览器验收、文档和回滚构建。

每轮继续提供“需求→代码→测试→浏览器证据→未验证项”矩阵，不得用占位页进入下一轮。

## 15. v3.0 验收门槛

- 2.0 完整数据迁移、幂等、失败不变各 100 次；
- 360 人 `personId` 唯一，复制新学期保持 person 但生成新学期学生 ID；
- 同名学生不自动关联；错误关联可安全解除；
- 备注新增、修改、置顶、完成、归档、恢复、永久删除前安全副本；
- 纯文本 XSS 攻击样例 100 个全部作为文本显示；
- 离线连续修改 100 次不产生重复备注；
- 两设备同一备注冲突不静默覆盖；
- 5,000 条备注/1,000 目标/1,000 跟进性能和容量测试；
- 档案入口、课堂快捷备注、今日跟进、时间线过滤和手机操作浏览器验收；
- 旧考勤、成绩、报表、PWA、2.0 同步和 10,000 次随机统计全部回归；
- 独立抽名单页构建与导入结果不包含备注、目标或任何 Supabase 代码。

## 16. 文档与维护

- 《使用与维护手册》增加学生档案、备注规范、跟进流程、跨学期关联、隐私边界、导出和恢复。
- 增加“建议记录/不建议记录”正反例，全部使用虚构信息。
- 维护说明记录 3.0 迁移、容量监控、备注归档和历史恢复。
- README、需求映射、测试报告、SQL 设置说明和根 MEMORY 同步更新。

## 17. 继续不做

- 学生、家长或班主任共享登录；
- 自动行为画像、心理诊断、风险评分、面部识别或位置追踪；
- 私人通讯录、医疗档案、处分档案或家庭背景数据库；
- 自动发送提醒、短信、邮件或即时消息；
- AI 自动撰写评价并直接保存到学生档案。
