# 教师课堂管理系统 v2.0 产品升级需求

> 日期：2026-08-11  
> 状态：已冻结，必须在 v1.2.5 稳定基线通过后实施  
> 原则：v2.0 必须带来可验证的产品变化，不得只修改版本号、配色或文案。

## 1. 2.0 的核心目标

系统从“分散的数据管理页面”升级为“教师上课工作台”：教师进入课堂后，不必在名单、考勤、成绩和抽名页面之间反复选择同一学期、班级和课程；同时能清楚知道数据是否保存、是否同步、是否需要备份以及如何恢复。

2.0 必须同时达到：

- 高频课堂操作路径明显缩短；
- 云同步、断网和冲突状态对非技术教师可理解；
- 误删、误覆盖和版本冲突具备可操作的恢复路径；
- 桌面与手机形成统一、成熟、克制的视觉系统；
- v1.1/v1.2 数据可以无损升级，独立抽名页仍可完全离线使用。

## 2. 五项实质性变化

### 2.1 新增“课堂工作台”

- 教师选择一次“学期＋班级＋课程”后，在课堂工作台中保持该上下文。
- 同一页面提供：开始/继续本次考勤、异常学生快速标记、快速抽名、抽中后记录课堂表现、查看本班预警与本节保存状态。
- 最近使用的教学组合可以从总览一键继续；归档学期只能查看，不出现误导性的可编辑入口。
- 不删除原有专业管理页面；课堂工作台负责高频操作，原页面负责批量维护和详细报表。

量化门槛：

- 已有最近教学组合时，从总览进入可编辑考勤不超过 2 次主要点击。
- 抽中学生后记录一条课堂表现不超过 2 次主要点击（选择预设理由可 1 次完成）。
- 工作台切换模块时不得重复选择学期、班级或课程。

### 2.2 新增“数据健康与恢复中心”

- 把同步状态、当前 revision、最近成功同步、待同步修改、最近 JSON 备份和本机安全副本集中展示。
- 教师可查看最近的本机安全副本，并逐个导出。
- 云模式下可查看 Supabase 最近 20 个历史版本的摘要；恢复历史版本必须先导出当前本机版本、二次确认并通过 CAS 生成新 revision，不得直接改写历史表。
- 提供清晰的“重试同步”“重新连接”“导出当前版本”“查看冲突”入口。
- 免费服务休眠、网络不可达、账号过期和真实冲突使用不同提示，不得都写成“同步失败”。

### 2.3 新增首次使用与升级引导

- 第一次打开 2.0 时，根据运行环境进入：本地模式说明、云账号登录或旧数据升级。
- 旧 v1.1/v1.2 本机数据先显示摘要，再由教师确认升级；不得自动上传。
- 云端和本机均有不同数据时进入双版本比较，不自动合并或覆盖。
- 引导只在必要时出现，完成后不得每次启动重复打扰；可从帮助页面重新打开。
- 引导中的每一步都说明“数据保存在哪里”和“下一步会不会覆盖”。

### 2.4 新增全局学生快速查找与上下文跳转

- 可按姓名或非空学号查找当前学期学生。
- 结果显示班级与课程上下文，可跳转到该学生的考勤/成绩明细。
- 键盘和触控均可使用；输入防抖，360 人数据下搜索反馈目标小于 100ms。
- 不引入服务端全文检索，不上传额外搜索索引。

### 2.5 建立 2.0 视觉与文案系统

- 继续使用青绿色主色，但统一为语义设计变量：背景、表面、正文、辅助字、边框、主操作、成功、警告、危险、焦点和图表色。
- 总览优先显示“下一步做什么”和数据健康；减少装饰性大卡片，增强信息层级。
- 课堂工作台使用适合投屏但不暴露全班成绩的布局；含成绩和隐私数据的详细页面明确标记“请勿投屏”。
- 动画只用于页面状态、抽名和保存反馈，尊重 `prefers-reduced-motion`。
- 所有用户文字进行人工审校：按钮使用动作词，错误说明包含发生原因、数据是否已保存和下一步。

## 3. 数据模型与迁移

- 数据版本升级为 `2.0`，提供显式 `1.1/1.2 -> 2.0` 迁移函数。
- 迁移必须保持所有 ID、名单快照、归档只读、考勤、成绩配置、课堂表现、抽名历史和设置。
- 新增字段必须有可解释默认值，禁止通过删除未知旧字段“解决”校验失败。
- 同一份旧数据连续迁移两次结果一致；失败不得改动原 localStorage 或云端记录。
- SQL 的基本版本检查同步支持 `2.0`，历史版本仍只读。
- JSON 导出明确标记 2.0；独立随机抽名页仍能只读取所需名单信息，不读取或展示成绩。

## 4. 可靠性与安全门槛

- 继续采用本地优先、CAS revision、RLS 和账号隔离，禁止静默 last-write-wins。
- 云历史恢复、首次升级、整库 JSON 恢复和“以本机覆盖云端”都必须生成可恢复副本。
- 退出账号后页面立即清空上一账号数据；存在未同步修改时继续阻止退出。
- Service Worker 必须通过在线升级不白屏、断服冷刷新可打开、Supabase 请求不缓存三项实测。
- 仓库和构建产物不得出现 service_role、真实 URL/key、真实学生数据或测试账号。
- 独立随机抽名页不得引入 Supabase SDK、账号、云同步或外部资源。

## 5. 性能、可访问性与响应式

- 本地模式入口主包不得回退到优化前水平；目标 raw 小于 300KB、gzip 小于 95KB。
- Excel、PDF 和 Supabase SDK 继续按需加载。
- 360 人虚构数据下，课堂工作台切换与学生搜索无明显卡顿；搜索测试至少执行 1,000 次并报告 p50/p95。
- 375px 宽度不得出现页面级横向溢出；高频触控目标至少 48px。
- 核心页面无无名称按钮、无标签表单项；同步状态需要读屏播报。
- 所有核心操作可以仅用键盘完成，危险操作不能因按 Enter 一次而直接执行。

## 6. 2.0 迭代顺序

Builder 不得一次性大改后再测试，按以下顺序逐轮交付：

1. `2.0-alpha`：数据版本迁移、课堂上下文与工作台骨架；
2. `2.0-beta1`：数据健康/云历史恢复和首次使用引导；
3. `2.0-beta2`：全局学生查找、操作路径与移动端；
4. `2.0-rc1`：视觉系统、文案、动效和可访问性；
5. `2.0`：全量回归、攻击测试、文档、截图和可回滚构建。

每轮必须记录：发现的问题、改动文件、前后指标、自动测试、浏览器结果和剩余风险。

## 7. 2.0 验收测试

除继续通过 v1.2.5 全部测试外，至少增加：

- 旧版完整虚构数据迁移、幂等迁移、失败原数据不变各 100 次；
- 课堂工作台上下文在考勤、抽名和课堂表现间保持一致；
- 最近教学组合两次点击内进入考勤；
- 抽名后两次点击内形成一次且仅一次课堂表现记录；
- 云历史列表账号隔离、历史恢复 CAS 冲突、恢复前安全副本；
- 1,000 次学生搜索性能和正确性，空学号及同名学生不得混淆；
- 375px 手机、桌面、键盘、读屏语义、减少动效；
- PWA 在线升级、断服刷新、按需资源首次使用与离线再次使用；
- 10,000 次纯随机与加权随机统计继续满足原门槛；
- 独立单页无 Supabase、无外链、无 secret 痕迹。

真实 Supabase 双设备延迟、RLS 和恢复功能仍必须在受控测试账号上线后单独验收；本地假服务结果不得冒充真实云验收。

## 8. 交付物

- 更新后的主系统、生产构建和独立随机抽名页；
- `V2_REQUIREMENTS.md` 逐项映射；
- 2.0 数据迁移、SQL 增量迁移和回滚说明；
- 2.0 各轮测试记录与最终 `TEST_REPORT.md`；
- 最终桌面、手机、课堂工作台、数据健康、登录/冲突和独立抽名截图；
- 按 2.0 最终界面重写并校验《使用与维护手册》；
- `README.md`、`REQUIREMENTS_MAPPING.md` 和根 `MEMORY.md` 更新。

## 9. 继续不做

- 学生账号、学生自拍签到、人脸识别或定位签到；
- 家长端、管理员后台、多教师共享编辑；
- 自动排课、AI 自动评分或无法解释的数据合并；
- 为追求“全新”而替换稳定技术栈、引入大型 UI 框架或重写全部业务逻辑。
