# 教师课堂管理系统 v1.2 实时同步增量需求

> 日期：2026-08-11  
> 状态：已冻结，交由 Builder 实现  
> 本文只覆盖主系统的“同一教师多设备实时同步”；未改动的 v1.1 需求继续有效。

## 1. 目标与验收口径

教师使用同一账号在 Windows、iPhone、iPad 登录后：

- 任一在线设备保存的名单、考勤、成绩、设置和归档数据，应在另一在线设备收到数据库变更后自动更新。
- 断网时仍能读取本机缓存并继续记录；恢复联网后自动补传，界面必须明确显示状态。
- 任何冲突、失败或非法远端数据都不得静默覆盖教师数据。
- 免费试运行采用 Cloudflare Pages + Supabase Free；项目代码不得包含任何私密凭据。

量化目标：

- 正常网络下，同一账号另一设备在收到 Realtime 事件后的 3 秒内完成界面更新。
- 连续 100 次离线修改后重连，只产生 1 份最终有效云端状态，不重复业务记录。
- 100 次过期 revision 写入全部被拒绝，静默覆盖次数必须为 0。
- 100 份畸形远端 payload 全部被拒绝，本机最后一个有效数据副本保持不变。

## 2. 冻结架构

为减少迁移风险，首版不拆分现有业务实体，采用“每名教师一份完整 JSON 文档”：

- `teacher_databases`：`owner_id` 为主键，保存 `payload`、单调递增的 `revision` 和更新时间。
- 通过数据库 RPC 执行 compare-and-swap：只有 `expected_revision` 等于当前 revision 才允许写入并递增版本。
- Supabase Auth 负责账号会话；主系统只提供登录，不开放页面自助注册。
- Supabase Realtime 订阅当前教师自己的数据库行。
- Postgres RLS 强制 `owner_id = auth.uid()`；匿名用户没有任何业务数据权限。
- 浏览器继续保存本地有效副本和待同步快照，以支持离线课堂。

客户端只能配置 `VITE_SUPABASE_URL` 和 Supabase publishable key。严禁在网页、测试、文档、日志或 PWA 缓存清单中出现 `service_role`、密码、token 或私钥。

## 3. 登录与本地数据迁移

- 未配置 Supabase 环境变量时，系统保持 v1.1 本地模式，全部原功能必须可用。
- 已配置云端时，未登录不得读取、显示或修改云端业务数据。
- 首次登录若云端为空而本机存在旧 v1.1 数据，不得自动上传；显示摘要并由教师明确选择“上传本机数据”或“使用空白云端”。
- 若云端与本机都有不同数据，不自动合并；进入冲突处理，先允许分别导出本机与云端 JSON。
- 退出登录前若存在未同步修改，必须阻止直接退出，并要求重试同步或先导出本机备份。
- 云端模式的本地缓存按 `user_id` 隔离；退出成功后不得在页面继续显示上一账号数据。

## 4. 本地优先同步状态机

界面固定显示以下状态之一，并显示最近成功同步时间：

- 未配置云同步
- 未登录
- 正在同步
- 已同步
- 离线，存在待同步修改
- 同步冲突
- 同步失败，可重试

规则：

1. 所有修改先通过现有 `validateDatabase`，原子写入本机，再以短防抖形成“最终快照”待同步。
2. 在线写入使用当前 revision 调用 compare-and-swap；成功后更新本地 revision。
3. 断网或暂时失败保留待同步快照，重连后自动重试；不得重复新增考勤、事件或抽名记录。
4. 收到更高 revision 的远端数据时，先完整校验，再原子替换本机；收到相同或更低 revision 时忽略。
5. 本机有待同步修改时收到更高 revision，立即停止自动覆盖并进入冲突状态。
6. Realtime 断线不能显示“已同步”；需要自动重订阅并提供手动重试。
7. 将远端数据应用到 React 状态时不得再次触发上传回环。

## 5. 冲突与误删保护

- 禁止 last-write-wins 静默覆盖。
- 冲突页必须显示本机版和云端版的 revision、更新时间、班级数、学生数及考勤表数。
- 提供“导出本机版本”“导出云端版本”。
- “以云端为准”和“以本机覆盖云端”都必须二次确认；覆盖前自动保存一份可恢复的本机安全副本。
- 云数据库在每次成功覆盖前保存旧 payload 历史，至少保留最近 20 个版本；历史记录只允许所有者读取，不允许客户端直接修改。
- JSON 手动恢复仍保持先校验、再确认；在云端模式下恢复成功后作为一次受 revision 保护的同步提交。

## 6. 安全和隐私门禁

- 所有暴露表必须启用 RLS；SQL 迁移应可重复部署并包含最小权限策略。
- RPC 内部从 `auth.uid()` 获取 owner，客户端不得传入或伪造 owner_id。
- 非当前账号的读、写、Realtime 订阅和历史读取都必须被数据库策略拒绝。
- 错误提示不得回显 token、完整学生 payload 或内部 SQL。
- Service Worker 只缓存同源静态资源，不缓存 Supabase Auth、REST、RPC 或 Realtime 响应。
- 真实学生数据上线前必须取得学校授权；仓库、测试、截图和文档只使用虚构数据。

## 7. 免费版限制的产品提示

“备份与设置”页面应明确说明：

- 免费云服务可能休眠、限额或调整政策，不等同于永久免费的正式 SLA。
- Supabase Free 没有可依赖的自动备份承诺；JSON 导出仍是必要的独立恢复手段。
- 海外服务在校园网络中的速度与稳定性需实测。
- 云同步失败不等于本机保存失败；教师应根据状态提示处理。

## 8. 独立随机抽名页面边界

- `deliverables/独立随机抽名.html` 继续完全离线、无账号、无 Supabase 依赖、无实时同步。
- 构建产物不得包含 Supabase URL、publishable key、SDK 或网络请求代码。
- 仍只通过教师主动选择 JSON 文件复用名单。

## 9. 交付物与测试

Builder 必须交付：

- Supabase SQL 迁移：表、历史表、索引、RLS、策略、compare-and-swap RPC 和历史保留逻辑。
- 云端配置示例文件，只含变量名和安全说明，不含真实值。
- 可单元测试的同步状态机/适配层，不把同步逻辑全部塞进 React 页面。
- 登录、同步状态、首次迁移、冲突解决和退出保护界面。
- Supabase 建项、建教师账号、执行迁移、配置 Cloudflare Pages、恢复与回滚说明。
- 自动化测试覆盖正常同步、断网补传、重复事件、非法 payload、revision 冲突、账号隔离和未配置回退。
- 更新 `README.md`、`REQUIREMENTS_MAPPING.md`、`TEST_REPORT.md` 和根目录 `MEMORY.md`。

在没有真实 Supabase 项目凭据时，允许使用严格的内存假服务完成自动化测试；但不得把“假服务通过”表述为真实云端已上线。最终上线验收仍需一个受控测试账号进行双浏览器实测。

## 10. 明确不做

- 学生账号、学生端页面、学生自行签到。
- 多教师共享同一班级或管理员后台。
- 自动排课、消息推送、家长端。
- 不可解释的自动数据合并。
- 独立随机抽名页面云同步。
